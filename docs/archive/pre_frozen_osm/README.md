# Pre-Frozen OSM Result Archive

This folder is reserved for historical IRP result files produced before the OSM travel matrix was frozen.

Do not use these files as final thesis evidence unless they are explicitly revalidated under the frozen travel matrix.

For final results, use `docs/results_frozen/`.
